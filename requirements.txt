# ──────────────────────────────────────────────────────────────────
# Python dependencies for the Sionna RT ↔ ns-3 bridge (ns3-sionna venv)
# Install: python3.12 -m venv ns3-sionna && source ns3-sionna/bin/activate && pip install -r requirements.txt
#
# Version-pinned to match sionna-oai-flexric-smo's verified environment
# (Sionna RT 2.0.1), so scene/RSRP code ported from that project behaves
# identically here.
# ──────────────────────────────────────────────────────────────────

# Sionna RT (ray tracing + channel simulation)
# NVIDIA GPU + CUDA 12+ required
sionna==2.0.1
sionna-rt==2.0.1

# ZMQ (ns-3 pathloss bridge UDP companion, GUI position/KPI relay)
pyzmq>=25.0

# Numerics
numpy>=1.24

# Jupyter notebook
jupyter>=1.0
jupyterlab>=4.0
ipykernel>=6.0
ipython>=8.0

# Visualization
matplotlib>=3.7
pandas>=2.0

# GUI (Polyscope 3D visualization, reused from sionna-oai-flexric-smo/gui)
polyscope>=2.6
drjit>=1.3
mitsuba>=3.8
